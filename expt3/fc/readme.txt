1. mnist_prefer_pathnet.py implements the internal preferential attachment like the pathnet;
2. mnist_prefer_pnn.py implements the external preferential attachment like the progressive neural network.
